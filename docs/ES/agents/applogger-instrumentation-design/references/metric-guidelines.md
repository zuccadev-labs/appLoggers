# Metric Guidelines

1. Always include unit.
2. Keep metric names stable and snake_case.
3. Track p95-sensitive timings where possible.
4. Add contextual tags only when they help triage.

Examples:

1. `screen_load_time` in `ms`
2. `api_latency` in `ms`
3. `startup_duration` in `ms`